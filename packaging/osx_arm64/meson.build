project('Trinity', version : 'YYYY.MM.DD')

meson.add_install_script('install.sh')
install_data('Info.plist.tmp', rename : 'Info.plist', install_dir : 'Contents')
install_data('logo.icns', rename : 'Trinity.icns', install_dir : 'Contents/Resources')
